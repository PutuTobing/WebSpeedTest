// API server URL — auto-detects hostname so it works on any machine
// Override here if the API is on a different host or port
window.API_URL = window.location.protocol + '//' + window.location.hostname + ':3001';
